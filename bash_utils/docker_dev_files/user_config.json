{
    "no_cuda": "false",
    "mounts": {
        "${HOME}/.cargo/env": "${HOME}/.cargo/env:ro",
        "${HOME}/.gitconfig": "${HOME}/.gitconfig:ro",
        "${HOME}/.oh-my-zsh": "${HOME}/.oh-my-zsh:ro",
        "${HOME}/.pip/pip.conf": "${HOME}/.pip/pip.conf:ro",
        "${HOME}/.local/share/atuin/key": "${HOME}/.atuin-key:ro",
        "${HOME}/github/utils/bash_utils/docker_dev_files/atuin_config.toml": "${HOME}/.atuin_config/config.toml:ro",
        "${HOME}/github/utils/dotfiles/.tmux.conf": "${HOME}/.tmux.conf:ro",
        "${HOME}/github/utils/dotfiles/.zshrc_docker": "${HOME}/.zshrc:ro",
        "${HOME}/github/utils/dotfiles/.zprofile": "${HOME}/.zprofile:ro",
        "${HOME}/github/utils/dotfiles/.profile": "${HOME}/.profile:ro",
        "${HOME}/github/utils/dotfiles/.bashrc": "${HOME}/.bashrc:ro",
 	"${HOME}/data": "${HOME}/data"
    },
    "env_vars": {
        "RUST_BACKTRACE": "1",
        "ATUIN_CONFIG_DIR": "${HOME}/.atuin_config",
        "PIP_CONFIG_FILE": "${HOME}/.pip/pip.conf",
	"GITHUB_TOKEN": "${GITHUB_TOKEN}"
    },
    "devices": [
    ],
    "additional_flags": [
    ]
}
